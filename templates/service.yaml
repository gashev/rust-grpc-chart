apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-grpc-chart.fullname" . }}
  labels:
    {{- include "rust-grpc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rust-grpc-chart.selectorLabels" . | nindent 4 }}
