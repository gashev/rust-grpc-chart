apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rust-grpc-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rust-grpc-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rust-grpc-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
